@extends('layouts.nav')
@section('content')
        <section id="big-banner" class="big-banner">

            <div class="banner-content">

                <div class="container">

                    <div class="content">

                        <h1>FAQ</h1>
                        <p>Bootstrap ships with vanilla CSS,<br> but its source code utilizes.</p>

                    </div>

                </div>

            </div>

        </section>


        <section id="faq">

            <div class="container">

                <div class="row">

                    <div class="faq">

                        <div class="col-sm-12">

                            <h2 class="">1. Bootstrap ships with vanilla CSS, but its source CSSBootstrap ships with vanilla CSS, but its source code utilizes ?</h2>

                        </div>

                        <div class="answer-faq">

                            <h2 class="" style="color:#ef6452">1. Bootstrap ships with vanilla CSS, but its source CSSBootstrap ships with vanilla CSS, but its source code utilizes ?</h2>

                            <div class="col-sm-10 col-sm-offset-2">

                            <blockquote>"Quickly get started with precompiled CSS or build on the sourceBootstrap ships with vanilla CSS. CSSBootstrap ships with vanilla CSS, but its source code utilizes the two most popular CSS preprocessors, Less and Sass. Quickly get started with precompiled CSS or build on the sourceBootstrap ships with vanilla CSS, but its source code utilizes the two most popular CSS preprocessors, Less and Sass. Quickly get started with precompiled CSS or build on the sourceBootstrap ships with vanilla CSS, but its"</blockquote>

                            </div>

                        </div>

                    </div>

                    <div class="faq">

                        <div class="col-sm-12">

                            <h2 class="" >2. Bootstrap ships with vanilla CSS, but its source CSSBootstrap ships with vanilla CSS, but its source code utilizes ?</h2>

                        </div>

                        <div class="answer-faq">

                            <h2 class="" style="color:#ef6452">2. Bootstrap ships with vanilla CSS, but its source CSSBootstrap ships with vanilla CSS, but its source code utilizes ?</h2>

                            <div class="col-sm-10 col-sm-offset-2">

                            <blockquote>"Quickly get started with precompiled CSS or build on the sourceBootstrap ships with vanilla CSS. CSSBootstrap ships with vanilla CSS, but its source code utilizes the two most popular CSS preprocessors, Less and Sass. Quickly get started with precompiled CSS or build on the sourceBootstrap ships with vanilla CSS, but its source code utilizes the two most popular CSS preprocessors, Less and Sass. Quickly get started with precompiled CSS or build on the sourceBootstrap ships with vanilla CSS, but its"</blockquote>

                            </div>

                        </div>

                    </div>

                    <div class="faq">

                        <div class="col-sm-12">

                            <h2 class="">3. Bootstrap ships with vanilla CSS, but its source CSSBootstrap ships with vanilla CSS, but its source code utilizes ?</h2>

                        </div>

                        <div class="answer-faq">

                            <h2 class="" style="color:#ef6452">3. Bootstrap ships with vanilla CSS, but its source CSSBootstrap ships with vanilla CSS, but its source code utilizes ?</h2>

                            <div class="col-sm-10 col-sm-offset-2">

                            <blockquote>"Quickly get started with precompiled CSS or build on the sourceBootstrap ships with vanilla CSS. CSSBootstrap ships with vanilla CSS, but its source code utilizes the two most popular CSS preprocessors, Less and Sass. Quickly get started with precompiled CSS or build on the sourceBootstrap ships with vanilla CSS, but its source code utilizes the two most popular CSS preprocessors, Less and Sass. Quickly get started with precompiled CSS or build on the sourceBootstrap ships with vanilla CSS, but its"</blockquote>

                            </div>

                        </div>

                    </div>

                    <div class="faq">

                        <div class="col-sm-12">

                            <h2 class="">4. Bootstrap ships with vanilla CSS, but its source CSSBootstrap ships with vanilla CSS, but its source code utilizes ?</h2>

                        </div>

                        <div class="answer-faq">

                            <h2 class="" style="color:#ef6452">4. Bootstrap ships with vanilla CSS, but its source CSSBootstrap ships with vanilla CSS, but its source code utilizes ?</h2>

                            <div class="col-sm-10 col-sm-offset-2">

                            <blockquote>"Quickly get started with precompiled CSS or build on the sourceBootstrap ships with vanilla CSS. CSSBootstrap ships with vanilla CSS, but its source code utilizes the two most popular CSS preprocessors, Less and Sass. Quickly get started with precompiled CSS or build on the sourceBootstrap ships with vanilla CSS, but its source code utilizes the two most popular CSS preprocessors, Less and Sass. Quickly get started with precompiled CSS or build on the sourceBootstrap ships with vanilla CSS, but its"</blockquote>

                            </div>

                        </div>

                    </div>

                </div>

            </div>

        </section>

        <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->

@endsection
</html>
